
export const parseChat = async({
    prompts,
    answers
  }) => {
    try{
        // const blocks: any[] = []
        for (let i = 0; i < prompts.length; i++) {
            // const { answerBlocks, promptBlocks } = generateBlocks(
            //     prompts[i],
            //     answers[i],
            //     true
            // )
            // const promptText = generateBlocks(prompts[i], answers[i], true)
        }
        
        return null;
    }
    catch(error){
        console.error(error)
    }
}
